package 剑指offer.数组中数据出现次数;

public class Code {
    /**
     * 在一个数组 nums 中除一个数字只出现一次之外，其他数字都出现了三次。
     * 请找出那个只出现一次的数字。
     * @param nums
     * @return
     */
    public int singleNumber(int[] nums) {

        return 0;
    }
}
